<% provide(:title, "All users") %>

<h1>All users</h1>
<body id="home">
	<div class="hero-unit">
		<%= will_paginate %>

		<ul class="users">
				<li>
					<%= render @users %>
				</li>
		</ul>	

		<%= will_paginate %>
	</div>
</body>
